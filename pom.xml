<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rynix</groupId>
    <artifactId>RynixRtpq</artifactId>
    <version>2.1.0-JAVA21-25</version>
    <packaging>jar</packaging>

    <name>RynixRtpq</name>
    <description>RynixRtpq Duo - 2 Players Same Location - Java 21-25 &amp; All MC Versions - Folia Supported | Author RinZz | Rynix Studio</description>

    <properties>
        <!-- Java 21-25 Support: Compile with Java 21 (LTS) - Works on Java 21,22,23,24,25 -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Paper API 1.20.4 - Compatible with Spigot 1.16+ via reflection, Folia 1.19.4+ -->
        <!-- Using Paper API for Folia support, but code uses reflection for backward compat -->
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.20.4-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <!-- Java 21-25: Use release 21 for maximum compatibility with Java 21-25 -->
                    <release>21</release>
                    <source>11</source>
                    <target>11</target>
                    <compilerArgs>
                        <arg>-Xlint:-options</arg>
                    </compilerArgs>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Multi-release JAR for Java 8-21 legacy support (optional) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Multi-Release>true</Multi-Release>
                            <Built-By>RinZz - Rynix Studio</Built-By>
                            <Implementation-Title>RynixRtpq</Implementation-Title>
                            <Implementation-Version>${project.version}</Implementation-Version>
                            <Implementation-Vendor>RinZz</Implementation-Vendor>
                            <Specification-Title>RynixRtpq Duo</Specification-Title>
                            <Sealed>false</Sealed>
                            <!-- Java 21-25 Support Tag -->
                            <Java-Version>21-25</Java-Version>
                            <Minecraft-Versions>1.8-1.21.5+</Minecraft-Versions>
                            <Folia-Supported>true</Folia-Supported>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
</project>
